following_grid_category:
  type: list
  list_type: category
  default: ""
  description: "list_of_category_slugs_or_ids"

vote_button_text:
  default: "Theo dõi"
  description: "text_for_unvoted_state"
  type: string

voted_button_text:
  default: "Đang theo dõi"
  description: "text_for_voted_state"
  type: string

enable_bulk_select:
  default: true
  description: "Enable bulk selection of topics in grid view"
  type: bool

bulk_select_max_items:
  default: 0
  description: "Maximum number of items that can be selected at once (0 = unlimited)"
  type: integer
